[package]
name = "luft-skills"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
keywords = ["luft", "agent", "orchestration", "skill"]
categories = ["asynchronous"]
description = "Compiled-in workflow authoring skill for Luft agents"

[dependencies]
luft-core = { path = "../luft-core", version = "0.3.4" }
const_format = "0.2"

[dev-dependencies]
tempfile = "3"
